;; A rest case reads every argument as one element type, so there is nothing
;; for a second declared type to describe.
;; EXPECT-ERROR: the case declares exactly one type
(import (std prelude))

(type (: Q (Union (CFrom Symbol string #:tag from #:rest)
                  (CPlain (List Symbol)))))

(defun (main args) 0)
