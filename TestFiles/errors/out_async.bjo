;; A method with out parameters is synchronous.
;; EXPECT-ERROR: out parameters cannot be async
(import (std prelude))

(import/extern
  (parse (: System.Int32.TryParse (-> string (out int) (Option int)) #:async #:uncancellable)))

(defun (main args) 0)
