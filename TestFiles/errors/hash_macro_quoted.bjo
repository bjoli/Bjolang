;; A hash macro call inside a quoted list: data cannot hold one.

;; EXPECT-ERROR: '#fl(...)' inside a quoted list
;; EXPECT-ERROR: write ,#fl(...)

(import (std prelude))

(defun (main)
  (println (list-length '(#fl(+ 1 2) 3)))
  0)
