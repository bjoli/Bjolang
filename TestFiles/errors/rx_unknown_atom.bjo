;; The other way round: a combinator spelled as a keyword.

;; EXPECT-ERROR: There is no rx called :seq

(import (std prelude))
(import (std rx))

(defun (main)
  (println (->str (rx-match? #rx(:seq) "a")))
  0)
