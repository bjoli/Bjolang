;; Rule 1, at a constructor. A constructor follows its type, so it is no more
;; filterable than the type is.
;;
;; EXPECT-ERROR: Invalid (except ...)
;; EXPECT-ERROR: 'MkCrate' is a constructor

(import (except "../inc/kitchen.bjo" MkCrate))

(defun (main)
  0)
