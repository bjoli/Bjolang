;; The default is the binder's value, so it has the binder's type and not the
;; scrutinee's.

;; EXPECT-ERROR: `:default` gives port its value, so it has port's type

(import (std prelude))

(: f (-> (Option int) int))
(defun (f o)
  (def (Some port) o :default None)
  port)
