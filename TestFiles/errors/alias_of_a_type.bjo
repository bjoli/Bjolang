;; Rule 8: `(:alias ...)` shares a *binding*, and a type has none to share. A
;; type has aliases of its own, and `prefix-types` renames a whole module's.
;;
;; EXPECT-ERROR: Alias Error: 'Crate'
;; EXPECT-ERROR: is a type

(import "../inc/kitchen.bjo")

(:alias Box Crate)

(defun (main)
  0)
