;; A prefix has to lex as part of the name it builds. Whitespace does not, so
;; the binding it would produce could not be written by hand.
;; EXPECT-ERROR: it contains whitespace, which cannot appear inside a symbol
(import (prefix (std prelude) "a b/"))

(defun (main) 0)
