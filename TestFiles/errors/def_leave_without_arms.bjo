;; `:leave` is followed by the arms that decide the body's value.

;; EXPECT-ERROR: `:leave` takes at least one arm

(import (std prelude))

(: f (-> (Option int) int))
(defun (f o)
  (def (Some n) o :leave)
  n)
