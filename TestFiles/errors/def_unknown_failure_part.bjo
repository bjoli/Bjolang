;; Only the four failure parts are keywords a def takes.

;; EXPECT-ERROR: `:fail` is not a failure part

(import (std prelude))

(: f (-> (Option int) int))
(defun (f o)
  (def (Some n) o :fail ((None 0)))
  n)
