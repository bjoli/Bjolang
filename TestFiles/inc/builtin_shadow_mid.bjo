;; Mellanledet. Programmet importerar bara den här, så modulen med skuggan nås
;; transitivt — och bidrar ändå med ett `using static`.

(export relayed)
(import (std prelude))
(import "builtin_shadow.bjo")

(: relayed (-> (List string) bool))
(defun (relayed xs) (shadowed-answer xs))
