;; An empty prefix renames nothing, and asking for it is more likely a mistake
;; than a request for a no-op.
;; EXPECT-ERROR: A prefix or postfix has to be a non-empty string
(import (prefix (std prelude) ""))

(defun (main) 0)
