;; A result is not a parameter.
;; EXPECT-ERROR: may only stand directly as a parameter of an import/extern signature
(import (std prelude))

(import/extern
  (parse-int (: System.Int32.TryParse (-> string (out int)))))

(defun (main args) 0)
