;; An unknown head, and the suggestion that covers the other spelling mistake.

;; EXPECT-ERROR: 'wat' is not an rx form

(import (std prelude))
(import (std rx))

(defun (main)
  (println (->str (rx-match? #rx(wat "a") "a")))
  0)
