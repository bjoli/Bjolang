;; The other way to write a facade: rename at the import rather than aliasing
;; after it. The name a `rename` produces is exportable for the same reason an
;; alias is — it already carries a signature from where it was defined.
(import (rename "net_internals.bjo" (ping/v2 knock)))

(export knock)
