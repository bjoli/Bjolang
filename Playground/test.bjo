(import (std prelude))
(import (std run))

(: tet Filter)
(defbjo (tet in out)
  (-> (read-all in)
      string-upcase
      (write-string out &))
  unit)

(defun (main args)
  (match (run/string '(from-file "Playground/test.bjo" ,tet))
    ((Ok s) (print s))
    ((Err e) (println e)))
  0)
