;; An import is an assembly boundary, so only exported names cross it.
;; `srcimport/helper.bjo` defines `secret` and does not export it.
;;
;; EXPECT-ERROR: Unbound variable: secret

(import "../srcimport/helper.bjo")

(defun (main args)
  (println (int->string (secret 1)))
  0)
