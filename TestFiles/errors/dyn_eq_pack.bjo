;; Samma regel vid packningen som vid annoteringen: verdiktet ligger på traiten,
;; så båda ställena ställer samma fråga och får samma svar.
;;
;; EXPECT-ERROR: 'Eq' cannot be boxed
;; EXPECT-ERROR: '=' mentions %a 2 times

(import (std prelude))

(defun (main args)
  (println (dyn Eq 42))
  0)
