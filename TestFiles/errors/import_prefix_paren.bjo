;; A parenthesis ends a symbol, so a prefix containing one splits the name it
;; was meant to build.
;; EXPECT-ERROR: cannot appear inside a symbol
(import (prefix (std prelude) "bad("))

(defun (main) 0)
