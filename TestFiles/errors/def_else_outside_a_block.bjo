;; A `def/else`'s else body leaves an enclosing `with-return`, so there has to
;; be one. Without it the form has nowhere to go.

;; EXPECT-ERROR: needs an enclosing

(import (std prelude))

(: f (-> (Option int) int))
(defun (f o)
  (def/else ((Some n) o)
    0)
  n)
