;; The Option form reads the outs on true, so the method has to return bool.
;; EXPECT-ERROR: The Option form needs a bool return
;; EXPECT-ERROR: int Math.DivRem(int a, int b, out int result)
(import (std prelude))

(import/extern
  (div-rem (: System.Math.DivRem (-> int int (out int) (Option int)))))

(defun (main args) 0)
