;; `(/ ...)` takes its endpoints in pairs, however they were spelled.

;; EXPECT-ERROR: A range takes its endpoints in pairs

(import (std prelude))
(import (std rx))

(defun (main)
  (println (->str (rx-match? #rx((/ #\a #\z #\0)) "a")))
  0)
