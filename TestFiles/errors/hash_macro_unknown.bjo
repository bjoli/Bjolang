;; A `#name(...)` with no hash macro behind it is a syntax error at the parser,
;; not an unbound name later.

;; EXPECT-ERROR: Unknown hash macro '#nope'

(import (std prelude))

(defun (main)
  (println #nope(1))
  0)
