;; (out T) describes a .NET out parameter, so a Bjolang signature has none.
;; EXPECT-ERROR: only an import/extern signature describes one
(import (std prelude))

(: f (-> (out int) int))
(defun (f x) x)

(defun (main args) 0)
