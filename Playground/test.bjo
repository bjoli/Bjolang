(import (std prelude))
(import (std stopwatch))

(defun (main args)
  (time-it #:times 500
           (loop (:for i (range 0 100000))
                 (:acc a (summing i))
                 => (println a)))
  0)   
