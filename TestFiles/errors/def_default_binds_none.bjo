;; A pattern that binds nothing has nothing for `:default` to give a value to.

;; EXPECT-ERROR: this pattern binds 0

(import (std prelude))

(: f (-> (Option int) int))
(defun (f o)
  (def None o :default 0)
  1)
