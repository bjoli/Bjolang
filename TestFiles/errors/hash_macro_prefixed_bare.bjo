;; A hash macro imported under a prefix is spelled with it: `#s/twice`, and the
;; bare `#twice` is nothing.

;; EXPECT-ERROR: Unknown hash macro '#twice'

(import (std prelude))
(import (prefix "../inc/hashmacros.bjo" "s/"))

(defun (main)
  (println #twice(4))
  0)
