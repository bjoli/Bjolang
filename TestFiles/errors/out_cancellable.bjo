;; A threaded token and outs do not combine.
;; EXPECT-ERROR: out parameters cannot be async
(import (std prelude))

(import/extern
  (parse (: System.Int32.TryParse (-> string (out int) (Option int)) #:cancellable)))

(defun (main args) 0)
