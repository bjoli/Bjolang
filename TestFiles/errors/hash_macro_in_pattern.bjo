;; A hash macro expands in expression position only.

;; EXPECT-ERROR: '#fl(...)' in a pattern
;; EXPECT-ERROR: expression position only

(import (std prelude))

(defun (main)
  (match (Tuple 1 2)
    (#fl(a b) (println "no"))
    (_ (println "no")))
  0)
