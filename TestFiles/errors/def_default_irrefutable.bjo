;; A `:default` on a pattern that always matches could never be used.

;; EXPECT-ERROR: always matches, so the failure part of this def can never run

(import (std prelude))

(: f (-> (Option int) (Option int)))
(defun (f o)
  (def whole o :default None)
  whole)
