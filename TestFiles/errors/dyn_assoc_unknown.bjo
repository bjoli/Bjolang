;; Vid dess deklarerade namn, och inget annat: felet räknar upp de namn traiten
;; faktiskt har.
;;
;; EXPECT-ERROR: 'Iterable' has no associated type #:item
;; EXPECT-ERROR: it has #:elem #:cursor

(import (std prelude))

(: total (-> (dyn Iterable #:item int) int))
(defun (total d) 0)

(defun (main args) 0)
