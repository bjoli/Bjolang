;; `only` and `except` name what the module offers. A name it does not is a
;; typo, and saying so beats silently importing nothing under it.
;;
;; EXPECT-ERROR: 'no-such-name' is not exported by kitchen.dll

(import (only "../inc/kitchen.bjo" no-such-name))

(defun (main)
  0)
