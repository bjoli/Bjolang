;; The other half of the same rule: a type that is not generic takes no
;; parameters, so an alias written applied to one is claiming an arity the type
;; does not have.
;; EXPECT-ERROR: is not a generic type
;; EXPECT-ERROR: Write the alias bare
(import (std prelude))

(import/class
  ((Writer %a) (: System.IO.StringWriter)))

(defun (main args) 0)
