;; Constructors with out parameters are not supported.
;; EXPECT-ERROR: constructors with out parameters are not supported
(import (std prelude))

(import/class
  (SB (: System.Text.StringBuilder (-> (out int) SB))))

(defun (main args) 0)
