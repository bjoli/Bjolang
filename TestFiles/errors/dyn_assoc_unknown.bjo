;; Vid dess deklarerade namn, och inget annat: felet räknar upp de namn traiten
;; faktiskt har.
;;
;; EXPECT-ERROR: 'Foldable' has no associated type #:elem
;; EXPECT-ERROR: it has #:item

(import (std prelude))

(: total (-> (dyn Foldable #:elem int) int))
(defun (total d) 0)

(defun (main args) 0)
