;; `#name` is a token only when a bracket follows it directly. Anything else
;; after `#` is still the reader's error, not a name.

;; EXPECT-ERROR: Unknown reader syntax '#fl'

(import (std prelude))

(defun (main)
  (println (#fl + 1 2 3))
  0)
