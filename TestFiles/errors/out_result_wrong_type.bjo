;; TryParse writes an int, and the declaration says long.
;; EXPECT-ERROR: The declared result must be exactly what the form produces
(import (std prelude))

(import/extern
  (parse (: System.Int32.TryParse (-> string (out int) (Option long)))))

(defun (main args) 0)
