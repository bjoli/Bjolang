;; A facade in front of a facade. An export resolves to its *ultimate* origin,
;; so this flattens: an importer of it reaches `net_internals` directly and
;; `net` is only a link-time dependency.
(import "net.bjo")

(:alias fetch http-get)

(export fetch)
