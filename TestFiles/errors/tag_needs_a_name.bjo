;; `#:tag` is not a marker on its own: it takes the name of the tag, which is
;; the whole of what it says.
;; EXPECT-ERROR: takes the name of the tag
(import (std prelude))

(type (: Q (Union (CFrom Symbol #:tag)
                  (CPlain (List Symbol)))))

(defun (main args) 0)
