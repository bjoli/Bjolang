;; (out T) stands directly as a parameter, never inside another type.
;; EXPECT-ERROR: may only stand directly as a parameter of an import/extern signature
(import (std prelude))

(import/extern
  (parse-int (: System.Int32.TryParse (-> string (Option (out int)) (Option int)))))

(defun (main args) 0)
